spring:
  application:
    name: analyzer  # Имя сервиса для Config Server.
      # По умолчанию Config Server вернёт analyzer.yml для профиля default.
      # При запросе http://localhost:8888/analyzer/default отдаст analyzer.yml.
    # Если обратиться к http://localhost:8888/analyzer/test,
    # попытается вернуть analyzer-test.yml + подмешать analyzer.yml.

  config:
    # Подключение к Config Server через псевдосхему "configserver:".
    # Если CONFIG_SERVER_URL не задана, используется http://localhost:8888.
    # Пример:
    #  - default профиль: http://localhost:8888/analyzer/default
    #  - test профиль:    http://localhost:8888/analyzer/test
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}

  cloud:
    config:
      # Если конфиг не загружен, приложение сразу падает (fail-fast).
      fail-fast: true

      # Настройка повторных попыток запроса к Config Server:
      retry:
        max-attempts: 5       # Кол-во попыток подключения
        initial-interval: 2000 # Пауза (мс) перед первой повторной попыткой
        max-interval: 10000    # Максимальный интервал пауз при экспоненциальном росте
